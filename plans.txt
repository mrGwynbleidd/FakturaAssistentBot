05-06-26
1. Собрать все PDF в data/pdf/.
2. Создать FAQ PDF по проблемам входа, пароля, ЭЦП и доступа.
3. Обновить prompt в generator.py.
4. Подключить query_rewriter.py в retriever.py.
5. Пересобрать индекс и протестировать 20 вопросов.


Отлично. Сейчас **не подключаем Telegram-группу**. Делаем сначала “мозг бота” локально:

```text
вопрос пользователя
   ↓
определение языка
   ↓
поиск по PDF/Chroma
   ↓
Gemini генерирует ответ
   ↓
сохранение вопроса и ответа в CSV
   ↓
ответ выводится в терминале
```

То есть мы создаём **ядро бота**, которое потом легко подключим к Telegram.

---

# Шаг 1. Текущая структура проекта

У тебя должно быть примерно так:

```text
faktura_bot/
│
├── app/
│   ├── bot/
│   │   └── language.py
│   │
│   ├── core/
│   │   └── bot_engine.py
│   │
│   ├── logs/
│   │   └── csv_logger.py
│   │
│   ├── rag/
│   │   ├── pdf_loader.py
│   │   ├── splitter.py
│   │   ├── embedding.py
│   │   ├── index_builder.py
│   │   ├── query_rewriter.py
│   │   ├── retriever.py
│   │   └── generator.py
│   │
│   └── config.py
│
├── data/
│   ├── pdf/
│   ├── chroma_db/
│   └── raw/
│       └── QA_outputs.csv
│
├── build_index.py
├── test_bot_engine.py
├── .env
└── requirements.txt
```

Теперь у нас есть нормальное ядро:

```text
test_bot_engine.py
        ↓
process_user_question()
        ↓
detect_language()
        ↓
retrieve_context()
        ↓
rewrite_query()
        ↓
Chroma search
        ↓
generate_answer()
        ↓
Gemini API
        ↓
save_qa()
        ↓
CSV log
```


вопрос пользователя
   ↓
бот не нашёл хороший ответ
   ↓
вопрос сохраняется как problem case
   ↓
админ потом пишет правильный ответ
   ↓
этот ответ сохраняется в базу обучения
   ↓
после подтверждения он добавляется в knowledge base
   ↓
пересоздаём embeddings
   ↓
в следующий раз бот уже отвечает лучше
```

PDF + approved cases → Chroma → Gemini → ответ
                         ↑
                         |
        user question + admin answer + review
```

### Do not save question in needs_review
add faktura API https://api.faktura.uz/docs/
Create tg-bot
take questions from needs_review auto, after review auto embedding 
interface for user and admin. Learn from real-user question and admins answer
https://t.me/fakturauz Assume, sometimes admins do not write it, sometimes very short
check cyrillic uzbek!!!
Photo embedding, understand photo and able to understand question from photo and mb little text from user 

###Need tg-bot that will kick and report mailing, ad bots, deleate process_user_question
Need dataset with keywords, and ability to detect it other languages


###learn git 


###agent, show cursor


!!!!!!!!!!!

Теперь вижу все проблемы:

**Проблема 1 — `needs_review.csv` не имеет заголовка** (`case_id,datetime,...`) — поэтому `csv.DictReader` не знает названия колонок и `row.get("status")` всегда возвращает `None`. Кейсы есть, но фильтр `== "needs_review"` никогда не срабатывает.

**Проблема 2 — `approved.csv`** — ответы с переносами строк ломают CSV потому что не экранируются правильно при записи.

**Проблема 3 — нет даты** в `approved.csv` — в `APPROVED_FIELDNAMES` нет поля `datetime`.

Исправляю все три файла + добавляю управление админами:

**`app/learning/case_logger.py`** — смотрю как пишется `needs_review.csv`:Теперь вижу всё. Проблема ясна — в `needs_review.csv` **нет заголовка** потому что файл уже существовал когда добавили `writer.writeheader()`. Поэтому первая строка это данные, а не заголовок — `DictReader` читает её как имена колонок и всё ломается.

Вот все исправления:

---

**`app/learning/case_logger.py`** — добавляем `quoting` чтобы многострочные ответы не ломали CSV:

```python
import csv
from pathlib import Path
from datetime import datetime

BASE_DIR = Path(__file__).resolve().parents[2]
NEEDS_REVIEW_PATH = BASE_DIR / "data" / "learning" / "needs_review.csv"

FIELDNAMES = [
    "case_id",
    "datetime",
    "language",
    "question",
    "bot_answer",
    "sources",
    "status",
    "reason",
    "admin_answer",
    "notes",
]


def format_sources(sources: list[dict] | None) -> str:
    if not sources:
        return ""
    result = []
    for source in sources:
        source_name = source.get("source", "unknown")
        distance = source.get("distance")
        if distance is not None:
            result.append(f"{source_name} | distance = {distance}")
        else:
            result.append(source_name)
    return "; ".join(result)


def create_case_id() -> str:
    return "case_" + datetime.now().strftime("%Y%m%d_%H%M%S_%f")


def save_case_for_review(
    question: str,
    bot_answer: str,
    language: str = "unknown",
    sources: list[dict] | None = None,
    reason: str = "unknown",
    notes: str = "",
) -> str:

    NEEDS_REVIEW_PATH.parent.mkdir(parents=True, exist_ok=True)
    file_exists = NEEDS_REVIEW_PATH.exists()

    # Если файл существует — проверяем есть ли заголовок
    has_header = False
    if file_exists:
        with open(NEEDS_REVIEW_PATH, encoding="utf-8-sig", newline="") as f:
            first_line = f.readline()
            has_header = "case_id" in first_line

    case_id = create_case_id()

    row = {
        "case_id": case_id,
        "datetime": datetime.now().strftime("%Y-%m-%d %H:%M:%S"),
        "language": language,
        "question": question,
        # Убираем переносы строк из ответа — они ломают CSV
        "bot_answer": bot_answer.replace("\n", " ").replace("\r", " ") if bot_answer else "",
        "sources": format_sources(sources),
        "status": "needs_review",
        "reason": reason,
        "admin_answer": "",
        "notes": notes.replace("\n", " ") if notes else "",
    }

    with open(NEEDS_REVIEW_PATH, mode="a", encoding="utf-8-sig", newline="") as file:
        writer = csv.DictWriter(
            file,
            fieldnames=FIELDNAMES,
            quoting=csv.QUOTE_ALL,  # все поля в кавычках — многострочный текст не ломает CSV
        )
        if not has_header:
            writer.writeheader()

        writer.writerow(row)

    return case_id
```

---

**`app/learning/review_manager.py`** — добавляем дату в `approved.csv`:

```python
import csv
from pathlib import Path
from datetime import datetime

BASE_DIR = Path(__file__).resolve().parents[2]
NEEDS_REVIEW_PATH = BASE_DIR / "data" / "learning" / "needs_review.csv"
APPROVED_CASES_PATH = BASE_DIR / "data" / "learning" / "approved.csv"

APPROVED_FIELDNAMES = [
    "case_id",
    "datetime",       # ← добавлено
    "language",
    "question",
    "approved_answer",
    "category",
    "source_type",
]


def approve_case_manually(
    case_id: str,
    question: str,
    approved_answer: str,
    language: str = "ru",
    category: str = "general",
) -> None:

    APPROVED_CASES_PATH.parent.mkdir(parents=True, exist_ok=True)

    file_exists = APPROVED_CASES_PATH.exists()

    # Проверяем есть ли заголовок
    has_header = False
    if file_exists:
        with open(APPROVED_CASES_PATH, encoding="utf-8-sig", newline="") as f:
            first_line = f.readline()
            has_header = "case_id" in first_line

    row = {
        "case_id": case_id,
        "datetime": datetime.now().strftime("%Y-%m-%d %H:%M:%S"),
        "language": language,
        "question": question,
        "approved_answer": approved_answer.replace("\n", " ").replace("\r", " ") if approved_answer else "",
        "category": category,
        "source_type": "admin_review",
    }

    with open(APPROVED_CASES_PATH, mode="a", encoding="utf-8-sig", newline="") as file:
        writer = csv.DictWriter(
            file,
            fieldnames=APPROVED_FIELDNAMES,
            quoting=csv.QUOTE_ALL,
        )
        if not has_header:
            writer.writeheader()

        writer.writerow(row)
```

---

**`app/bot/admin_handlers.py`** — исправляем чтение CSV + добавляем управление админами:

```python
import csv
from pathlib import Path
from datetime import datetime

from aiogram import Router, F
from aiogram.types import Message, ReplyKeyboardMarkup, KeyboardButton
from aiogram.filters import Command
from aiogram.fsm.context import FSMContext
from aiogram.fsm.state import State, StatesGroup

from app.learning.review_manager import approve_case_manually

BASE_DIR = Path(__file__).resolve().parents[2]
NEEDS_REVIEW_PATH = BASE_DIR / "data" / "learning" / "needs_review.csv"
APPROVED_CASES_PATH = BASE_DIR / "data" / "learning" / "approved.csv"
ADMINS_PATH = BASE_DIR / "data" / "admins.txt"

# Начальный админ — всегда имеет доступ
ROOT_ADMIN_ID = 677539972

router = Router()


# ── Управление админами ───────────────────────────────────────────────────────
def load_admin_ids() -> set[int]:
    ids = {ROOT_ADMIN_ID}
    if ADMINS_PATH.exists():
        with open(ADMINS_PATH, encoding="utf-8") as f:
            for line in f:
                line = line.strip()
                if line.isdigit():
                    ids.add(int(line))
    return ids


def save_admin_ids(ids: set[int]) -> None:
    ADMINS_PATH.parent.mkdir(parents=True, exist_ok=True)
    # ROOT_ADMIN не сохраняем — он всегда в коде
    other_ids = ids - {ROOT_ADMIN_ID}
    with open(ADMINS_PATH, "w", encoding="utf-8") as f:
        for admin_id in sorted(other_ids):
            f.write(f"{admin_id}\n")


def is_admin(user_id: int) -> bool:
    return user_id in load_admin_ids()


# ── FSM ───────────────────────────────────────────────────────────────────────
class AdminReview(StatesGroup):
    waiting_for_answer = State()
    waiting_for_category = State()
    waiting_for_new_admin_id = State()
    waiting_for_remove_admin_id = State()


ADMIN_SESSION: dict[int, dict] = {}


# ── Клавиатуры ────────────────────────────────────────────────────────────────
def admin_main_keyboard() -> ReplyKeyboardMarkup:
    return ReplyKeyboardMarkup(
        keyboard=[
            [KeyboardButton(text="📋 Кейсы на проверку")],
            [KeyboardButton(text="📊 Статистика"), KeyboardButton(text="👥 Управление админами")],
            [KeyboardButton(text="🚪 Выйти из панели")],
        ],
        resize_keyboard=True,
    )


def review_action_keyboard() -> ReplyKeyboardMarkup:
    return ReplyKeyboardMarkup(
        keyboard=[
            [KeyboardButton(text="✅ Одобрить"), KeyboardButton(text="⏭ Следующий")],
            [KeyboardButton(text="❌ Отклонить"), KeyboardButton(text="📋 Все кейсы")],
            [KeyboardButton(text="🚪 Выйти из панели")],
        ],
        resize_keyboard=True,
    )


def cancel_keyboard() -> ReplyKeyboardMarkup:
    return ReplyKeyboardMarkup(
        keyboard=[[KeyboardButton(text="❌ Отмена")]],
        resize_keyboard=True,
    )


def admins_keyboard() -> ReplyKeyboardMarkup:
    return ReplyKeyboardMarkup(
        keyboard=[
            [KeyboardButton(text="➕ Добавить админа")],
            [KeyboardButton(text="➖ Удалить админа"), KeyboardButton(text="📋 Список админов")],
            [KeyboardButton(text="⬅️ Назад в панель")],
        ],
        resize_keyboard=True,
    )


# ── Чтение needs_review.csv ───────────────────────────────────────────────────
def load_pending_cases() -> list[dict]:
    if not NEEDS_REVIEW_PATH.exists():
        return []

    cases = []
    with open(NEEDS_REVIEW_PATH, encoding="utf-8-sig", newline="") as file:
        content = file.read()

    # Проверяем есть ли заголовок
    if "case_id" not in content.split("\n")[0]:
        # Заголовка нет — файл сломан, возвращаем пустой список
        return []

    import io
    reader = csv.DictReader(io.StringIO(content), quoting=csv.QUOTE_ALL)
    for row in reader:
        if row.get("status", "").strip() == "needs_review":
            cases.append(dict(row))

    return cases


def mark_case_as_reviewed(case_id: str, new_status: str) -> None:
    if not NEEDS_REVIEW_PATH.exists():
        return

    rows = []
    fieldnames = None

    with open(NEEDS_REVIEW_PATH, encoding="utf-8-sig", newline="") as file:
        reader = csv.DictReader(file, quoting=csv.QUOTE_ALL)
        fieldnames = reader.fieldnames
        for row in reader:
            if row.get("case_id") == case_id:
                row["status"] = new_status
            rows.append(row)

    if not fieldnames:
        return

    with open(NEEDS_REVIEW_PATH, "w", encoding="utf-8-sig", newline="") as file:
        writer = csv.DictWriter(file, fieldnames=fieldnames, quoting=csv.QUOTE_ALL)
        writer.writeheader()
        writer.writerows(rows)


def get_stats() -> dict:
    stats = {"pending": 0, "approved": 0, "rejected": 0, "skipped": 0}

    if NEEDS_REVIEW_PATH.exists():
        with open(NEEDS_REVIEW_PATH, encoding="utf-8-sig", newline="") as file:
            for row in csv.DictReader(file, quoting=csv.QUOTE_ALL):
                status = row.get("status", "").strip()
                if status == "needs_review":
                    stats["pending"] += 1
                elif status == "rejected":
                    stats["rejected"] += 1
                elif status == "skipped":
                    stats["skipped"] += 1

    if APPROVED_CASES_PATH.exists():
        with open(APPROVED_CASES_PATH, encoding="utf-8-sig", newline="") as file:
            stats["approved"] = sum(1 for _ in csv.DictReader(file))

    return stats


def format_case_message(case: dict, index: int, total: int) -> str:
    lang_emoji = {"ru": "🇷🇺", "uz": "🇺🇿", "en": "🇬🇧"}.get(case.get("language", "ru"), "🌐")
    reason_map = {
        "weak_source_distance": "слабый контекст",
        "weak_answer_text": "слабый ответ",
        "empty_context": "нет контекста",
        "no_sources": "нет источников",
        "retriever_or_api_error": "ошибка поиска",
        "generator_error": "ошибка генерации",
    }
    reason = reason_map.get(case.get("reason", ""), case.get("reason", "—"))

    # Обрезаем длинный ответ бота чтобы не спамить
    bot_answer = case.get("bot_answer", "—")
    if len(bot_answer) > 300:
        bot_answer = bot_answer[:300] + "..."

    return (
        f"📌 *Кейс {index}/{total}*\n"
        f"🆔 `{case.get('case_id', '—')}`\n"
        f"🕐 {case.get('datetime', '—')}\n"
        f"{lang_emoji} Язык: `{case.get('language', '—')}`\n"
        f"⚠️ Причина: `{reason}`\n"
        f"───────────────────\n"
        f"❓ *Вопрос:*\n{case.get('question', '—')}\n"
        f"───────────────────\n"
        f"🤖 *Ответ бота:*\n{bot_answer}\n"
        f"───────────────────\n"
        f"📎 Источники: `{case.get('sources', 'нет')[:100]}`"
    )


# ── /admin ────────────────────────────────────────────────────────────────────
@router.message(Command("admin"))
async def admin_panel_handler(msg: Message, state: FSMContext):
    if not is_admin(msg.from_user.id):
        await msg.answer("⛔ У вас нет доступа к этой команде.")
        return
    await state.clear()
    pending = load_pending_cases()
    await msg.answer(
        f"👨‍💼 *Добро пожаловать в админ-панель!*\n\n"
        f"📋 Кейсов на проверку: *{len(pending)}*\n\n"
        f"Выберите действие:",
        reply_markup=admin_main_keyboard(),
        parse_mode="Markdown",
    )


# ── Выход ─────────────────────────────────────────────────────────────────────
@router.message(F.text.in_(["🚪 Выйти из панели", "⬅️ Назад в панель"]))
async def exit_admin_panel(msg: Message, state: FSMContext):
    if not is_admin(msg.from_user.id):
        return
    await state.clear()
    ADMIN_SESSION.pop(msg.from_user.id, None)

    if msg.text == "⬅️ Назад в панель":
        pending = load_pending_cases()
        await msg.answer(
            f"👨‍💼 *Админ-панель*\n\nКейсов на проверку: *{len(pending)}*",
            reply_markup=admin_main_keyboard(),
            parse_mode="Markdown",
        )
        return

    from app.bot.keyboards import main_menu_keyboard
    await msg.answer("Вы вышли из админ-панели.", reply_markup=main_menu_keyboard("ru"))


# ── Статистика ────────────────────────────────────────────────────────────────
@router.message(F.text == "📊 Статистика")
async def stats_handler(msg: Message):
    if not is_admin(msg.from_user.id):
        return
    stats = get_stats()
    await msg.answer(
        f"📊 *Статистика базы знаний*\n\n"
        f"⏳ Ожидают проверки: *{stats['pending']}*\n"
        f"✅ Одобрено: *{stats['approved']}*\n"
        f"❌ Отклонено: *{stats['rejected']}*\n"
        f"⏭ Пропущено: *{stats['skipped']}*",
        reply_markup=admin_main_keyboard(),
        parse_mode="Markdown",
    )


# ── Управление админами ───────────────────────────────────────────────────────
@router.message(F.text == "👥 Управление админами")
async def manage_admins(msg: Message, state: FSMContext):
    if not is_admin(msg.from_user.id):
        return
    await state.clear()
    await msg.answer("👥 *Управление администраторами*", reply_markup=admins_keyboard(), parse_mode="Markdown")


@router.message(F.text == "📋 Список админов")
async def list_admins(msg: Message):
    if not is_admin(msg.from_user.id):
        return
    ids = load_admin_ids()
    lines = [f"👑 `{ROOT_ADMIN_ID}` — главный админ"]
    for admin_id in sorted(ids):
        if admin_id != ROOT_ADMIN_ID:
            lines.append(f"👤 `{admin_id}`")
    await msg.answer(
        f"📋 *Список администраторов:*\n\n" + "\n".join(lines),
        reply_markup=admins_keyboard(),
        parse_mode="Markdown",
    )


@router.message(F.text == "➕ Добавить админа")
async def add_admin_start(msg: Message, state: FSMContext):
    if not is_admin(msg.from_user.id):
        return
    await msg.answer(
        "Отправьте Telegram *user_id* нового администратора.\n\n"
        "Узнать ID можно через @userinfobot",
        reply_markup=cancel_keyboard(),
        parse_mode="Markdown",
    )
    await state.set_state(AdminReview.waiting_for_new_admin_id)


@router.message(AdminReview.waiting_for_new_admin_id, F.text == "❌ Отмена")
async def cancel_add_admin(msg: Message, state: FSMContext):
    await state.clear()
    await msg.answer("Отменено.", reply_markup=admins_keyboard())


@router.message(AdminReview.waiting_for_new_admin_id, F.text)
async def add_admin_confirm(msg: Message, state: FSMContext):
    if not is_admin(msg.from_user.id):
        return
    text = msg.text.strip()
    if not text.isdigit():
        await msg.answer("❌ Введите числовой Telegram ID (только цифры).")
        return
    new_id = int(text)
    ids = load_admin_ids()
    if new_id in ids:
        await msg.answer(f"⚠️ Пользователь `{new_id}` уже является админом.", parse_mode="Markdown")
    else:
        ids.add(new_id)
        save_admin_ids(ids)
        await msg.answer(f"✅ Пользователь `{new_id}` добавлен как администратор.", parse_mode="Markdown")
    await state.clear()
    await msg.answer("Выберите действие:", reply_markup=admins_keyboard())


@router.message(F.text == "➖ Удалить админа")
async def remove_admin_start(msg: Message, state: FSMContext):
    if not is_admin(msg.from_user.id):
        return
    ids = load_admin_ids()
    removable = ids - {ROOT_ADMIN_ID}
    if not removable:
        await msg.answer("Нет дополнительных администраторов для удаления.")
        return
    lines = "\n".join(f"• `{i}`" for i in sorted(removable))
    await msg.answer(
        f"Отправьте ID админа для удаления:\n\n{lines}",
        reply_markup=cancel_keyboard(),
        parse_mode="Markdown",
    )
    await state.set_state(AdminReview.waiting_for_remove_admin_id)


@router.message(AdminReview.waiting_for_remove_admin_id, F.text == "❌ Отмена")
async def cancel_remove_admin(msg: Message, state: FSMContext):
    await state.clear()
    await msg.answer("Отменено.", reply_markup=admins_keyboard())


@router.message(AdminReview.waiting_for_remove_admin_id, F.text)
async def remove_admin_confirm(msg: Message, state: FSMContext):
    if not is_admin(msg.from_user.id):
        return
    text = msg.text.strip()
    if not text.isdigit():
        await msg.answer("❌ Введите числовой Telegram ID.")
        return
    remove_id = int(text)
    if remove_id == ROOT_ADMIN_ID:
        await msg.answer("⛔ Нельзя удалить главного администратора.")
        await state.clear()
        return
    ids = load_admin_ids()
    if remove_id not in ids:
        await msg.answer(f"⚠️ Пользователь `{remove_id}` не найден в списке.", parse_mode="Markdown")
    else:
        ids.discard(remove_id)
        save_admin_ids(ids)
        await msg.answer(f"✅ Пользователь `{remove_id}` удалён из администраторов.", parse_mode="Markdown")
    await state.clear()
    await msg.answer("Выберите действие:", reply_markup=admins_keyboard())


# ── Просмотр кейсов ───────────────────────────────────────────────────────────
@router.message(F.text.in_(["📋 Кейсы на проверку", "📋 Все кейсы"]))
async def start_review(msg: Message, state: FSMContext):
    if not is_admin(msg.from_user.id):
        return
    cases = load_pending_cases()
    if not cases:
        await msg.answer("✅ Все кейсы проверены, очередь пуста.", reply_markup=admin_main_keyboard())
        return
    ADMIN_SESSION[msg.from_user.id] = {"cases": cases, "index": 0}
    await send_current_case(msg, state)


async def send_current_case(msg: Message, state: FSMContext):
    session = ADMIN_SESSION.get(msg.from_user.id)
    if not session:
        return
    cases = session["cases"]
    index = session["index"]
    if index >= len(cases):
        await msg.answer("✅ Все кейсы просмотрены!", reply_markup=admin_main_keyboard())
        ADMIN_SESSION.pop(msg.from_user.id, None)
        await state.clear()
        return
    case = cases[index]
    text = format_case_message(case, index + 1, len(cases))
    await msg.answer(text, reply_markup=review_action_keyboard(), parse_mode="Markdown")


@router.message(F.text == "✅ Одобрить")
async def approve_start(msg: Message, state: FSMContext):
    if not is_admin(msg.from_user.id):
        return
    session = ADMIN_SESSION.get(msg.from_user.id)
    if not session:
        await msg.answer("Сначала откройте кейсы: «📋 Кейсы на проверку»")
        return
    case = session["cases"][session["index"]]
    await msg.answer(
        f"✏️ *Напишите правильный ответ* для вопроса:\n\n_{case.get('question', '')}_",
        reply_markup=cancel_keyboard(),
        parse_mode="Markdown",
    )
    await state.set_state(AdminReview.waiting_for_answer)


@router.message(AdminReview.waiting_for_answer, F.text == "❌ Отмена")
async def cancel_approve(msg: Message, state: FSMContext):
    await state.clear()
    await msg.answer("Отменено.", reply_markup=review_action_keyboard())


@router.message(AdminReview.waiting_for_answer, F.text)
async def receive_admin_answer(msg: Message, state: FSMContext):
    if not is_admin(msg.from_user.id):
        return
    await state.update_data(admin_answer=msg.text.strip())
    await state.set_state(AdminReview.waiting_for_category)
    await msg.answer(
        "📂 *Укажите категорию:*",
        reply_markup=ReplyKeyboardMarkup(
            keyboard=[
                [KeyboardButton(text="login"), KeyboardButton(text="registration")],
                [KeyboardButton(text="edo"), KeyboardButton(text="invoice")],
                [KeyboardButton(text="password"), KeyboardButton(text="general")],
                [KeyboardButton(text="⏩ Пропустить категорию")],
            ],
            resize_keyboard=True,
        ),
        parse_mode="Markdown",
    )


@router.message(AdminReview.waiting_for_category, F.text)
async def receive_category(msg: Message, state: FSMContext):
    if not is_admin(msg.from_user.id):
        return
    session = ADMIN_SESSION.get(msg.from_user.id)
    if not session:
        await state.clear()
        return
    data = await state.get_data()
    admin_answer = data.get("admin_answer", "")
    case = session["cases"][session["index"]]
    category = "general" if msg.text.strip() == "⏩ Пропустить категорию" else msg.text.strip().lower()

    approve_case_manually(
        case_id=case.get("case_id", f"manual_{datetime.now().strftime('%Y%m%d_%H%M%S')}"),
        question=case.get("question", ""),
        approved_answer=admin_answer,
        language=case.get("language", "ru"),
        category=category,
    )
    mark_case_as_reviewed(case.get("case_id", ""), "approved")
    await state.clear()

    session["index"] += 1
    remaining = len(session["cases"]) - session["index"]

    await msg.answer(
        f"✅ *Сохранено!* Категория: `{category}` | Осталось: *{remaining}*",
        reply_markup=review_action_keyboard(),
        parse_mode="Markdown",
    )
    if remaining == 0:
        await msg.answer("🎉 Все кейсы проверены!", reply_markup=admin_main_keyboard())
        ADMIN_SESSION.pop(msg.from_user.id, None)
    else:
        await send_current_case(msg, state)


@router.message(F.text == "⏭ Следующий")
async def skip_case(msg: Message, state: FSMContext):
    if not is_admin(msg.from_user.id):
        return
    session = ADMIN_SESSION.get(msg.from_user.id)
    if not session:
        return
    case = session["cases"][session["index"]]
    mark_case_as_reviewed(case.get("case_id", ""), "skipped")
    session["index"] += 1
    await send_current_case(msg, state)


@router.message(F.text == "❌ Отклонить")
async def reject_case(msg: Message, state: FSMContext):
    if not is_admin(msg.from_user.id):
        return
    session = ADMIN_SESSION.get(msg.from_user.id)
    if not session:
        return
    case = session["cases"][session["index"]]
    mark_case_as_reviewed(case.get("case_id", ""), "rejected")
    session["index"] += 1
    await msg.answer("❌ Кейс отклонён.", reply_markup=review_action_keyboard())
    await send_current_case(msg, state)
```

---

**Последний шаг — почини существующий `needs_review.csv`** вручную. Открой его в VS Code и добавь первой строкой:

```
case_id,datetime,language,question,bot_answer,sources,status,reason,admin_answer,notes
```

Или запусти этот одноразовый скрипт:

```python
# fix_csv_header.py — запусти один раз из корня проекта
from pathlib import Path

path = Path("data/learning/needs_review.csv")
content = path.read_text(encoding="utf-8-sig")

header = "case_id,datetime,language,question,bot_answer,sources,status,reason,admin_answer,notes\n"

if "case_id" not in content.split("\n")[0]:
    path.write_text(header + content, encoding="utf-8-sig")
    print("Заголовок добавлен!")
else:
    print("Заголовок уже есть.")
```

```bash
python fix_csv_header.py
```

---

**Итого что исправлено:**

| # | Проблема | Решение |
|---|---|---|
| 1 | Кейсы не отображались | `load_pending_cases` теперь проверяет наличие заголовка |
| 2 | Многострочные ответы ломали CSV | `quoting=csv.QUOTE_ALL` + убираем `\n` из полей |
| 3 | Нет даты в `approved.csv` | Добавлено поле `datetime` |
| 4 | Добавление/удаление админов | Хранятся в `data/admins.txt`, кнопка «👥 Управление админами» |










Для твоего проекта (**Telegram Bot + ChromaDB + PDF + Faktura API**) я бы рекомендовал не OpenClaw, а **Ollama + Qwen3 8B**.

Причина простая:

```text
Qwen3 8B
✓ русский
✓ узбекский
✓ английский
✓ хорошо работает с инструкциями
✓ легко подключается к Python
✓ бесплатно
✓ без лимитов
```

---

# Шаг 1. Проверить ноутбук

Открой PowerShell:

```powershell
systeminfo
```

или:

```powershell
Get-ComputerInfo
```

Напиши мне:

```text
CPU:
RAM:
GPU:
```

Но примерно:

### Если у тебя

```text
16 GB RAM
```

то запускаем:

```text
Qwen3:4b
```

---

### Если

```text
32 GB RAM
```

то:

```text
Qwen3:8b
```

---

### Если

```text
64 GB RAM
```

то:

```text
Qwen3:14b
```

---

# Шаг 2. Установка Ollama

Скачать:

[Ollama Official Website](https://ollama.com/download?utm_source=chatgpt.com)

Установить как обычную программу.

---

Проверить:

```powershell
ollama --version
```

Должно показать:

```text
ollama version ...
```

---

# Шаг 3. Скачать модель

Для начала рекомендую:

```powershell
ollama pull qwen3:4b
```

или

```powershell
ollama pull qwen3:8b
```

---

Размер примерно:

```text
4b ≈ 2.5-3 ГБ
8b ≈ 5-6 ГБ
```

---

# Шаг 4. Проверить модель

Запусти:

```powershell
ollama run qwen3:4b
```

Напиши:

```text
Как создать ТТН в Faktura?
```

Если отвечает — всё работает.

Выход:

```text
/bye
```

---

# Шаг 5. Установить библиотеку Python

В проекте:

```powershell
pip install ollama
```

---

# Шаг 6. Создать новый файл

Создай:

```text
app/llm/ollama_client.py
```

---

Код:

```python
from ollama import chat


def ask_ollama(prompt: str) -> str:
    response = chat(
        model="qwen3:4b",
        messages=[
            {
                "role": "user",
                "content": prompt,
            }
        ],
    )

    return response["message"]["content"]
```

---

# Шаг 7. Проверка из Python

Создай:

```text
test_ollama.py
```

Код:

```python
from app.llm.ollama_client import ask_ollama

answer = ask_ollama(
    "Что такое Faktura?"
)

print(answer)
```

Запуск:

```powershell
python test_ollama.py
```

---

# Шаг 8. Заменяем Gemini

Сейчас у тебя примерно есть:

```python
from app.llm.gemini_client import ask_gemini

answer = ask_gemini(prompt)
```

Меняем на:

```python
from app.llm.ollama_client import ask_ollama

answer = ask_ollama(prompt)
```

---

# Шаг 9. Проверяем весь RAG

Схема останется такой:

```text
Вопрос пользователя
        ↓
query_rewriter.py
        ↓
retriever.py
        ↓
ChromaDB
        ↓
контекст
        ↓
Ollama
        ↓
ответ
```

---

# Шаг 10. Настройка качества

Когда заработает:

```powershell
ollama pull qwen3:8b
```

и меняем:

```python
model="qwen3:4b"
```

на

```python
model="qwen3:8b"
```

Качество станет заметно лучше.

---

# Для твоего ноутбука

Я бы начал именно так:

```text
Ollama
+
Qwen3:4b
+
ChromaDB
+
Faktura API
```

Потому что:

* ставится за 10 минут;
* не требует сервер;
* не требует токены Google;
* позволяет проверить всю архитектуру локально;
* потом легко переносится на сервер без изменения логики проекта.

После установки пришли характеристики ноутбука (`CPU`, `RAM`, `GPU`), и я подскажу, какую максимальную модель ты сможешь комфортно запускать локально.




Если ты переходишь с **Gemini → Ollama + Qwen**, то хорошая новость: **80-90% проекта менять не придется**.

У тебя уже есть правильная архитектура:

```text
Telegram
↓
bot_engine.py
↓
retriever.py
↓
ChromaDB
↓
generator.py
↓
LLM
```

Меняется только последний блок:

```text
Gemini
↓
Qwen
```

---

# Файл №1 (создать)

## `app/llm/ollama_client.py`

Создай новый файл:

```python
from ollama import chat


def ask_ollama(prompt: str) -> str:
    response = chat(
        model="qwen3:4b",  # позже можно qwen3:8b
        messages=[
            {
                "role": "user",
                "content": prompt
            }
        ]
    )

    return response["message"]["content"]
```

---

# Файл №2

## `app/llm/generator.py`

Это главный файл.

Сейчас там примерно:

```python
from app.llm.gemini_client import ask_gemini
```

Меняем на:

```python
from app.llm.ollama_client import ask_ollama
```

---

Дальше ищем:

```python
answer = ask_gemini(prompt)
```

Меняем:

```python
answer = ask_ollama(prompt)
```

---

Пример:

Было:

```python
try:
    answer = ask_gemini(prompt)
except Exception as e:
    ...
```

Стало:

```python
try:
    answer = ask_ollama(prompt)
except Exception as e:
    ...
```

---

# Файл №3

## `requirements.txt`

Добавить:

```text
ollama
```

И выполнить:

```bash
pip install ollama
```

---

# Файл №4

## `.env`

Раньше:

```env
GEMINI_API_KEY=xxxxxxxx
```

После перехода можно оставить, но уже не использовать.

Дополнительно можно добавить:

```env
LLM_PROVIDER=ollama
OLLAMA_MODEL=qwen3:4b
```

---

# Файл №5 (рекомендую)

## `app/config.py`

Сделай единое место настроек:

```python
import os

LLM_PROVIDER = os.getenv("LLM_PROVIDER", "ollama")
OLLAMA_MODEL = os.getenv("OLLAMA_MODEL", "qwen3:4b")
```

---

Тогда в `ollama_client.py`:

```python
from ollama import chat
from app.config import OLLAMA_MODEL


def ask_ollama(prompt: str) -> str:
    response = chat(
        model=OLLAMA_MODEL,
        messages=[
            {
                "role": "user",
                "content": prompt
            }
        ]
    )

    return response["message"]["content"]
```

---

# Что НЕ нужно менять

Эти файлы остаются как есть:

### ChromaDB

```text
app/rag/retriever.py
```

---

### PDF индексатор

```text
app/rag/index_builder.py
```

---

### PDF loader

```text
app/rag/pdf_loader.py
```

---

### splitter

```text
app/rag/text_splitter.py
```

---

### Telegram

```text
app/bot/handlers.py
```

---

### Faktura API

```text
app/api/*
```

---

### Review system

```text
app/review/*
```

---

### CSV logger

```text
app/logs/*
```

---

# Что желательно улучшить после перехода

В `generator.py` сейчас, вероятно, промпт писался под Gemini.

Для Qwen лучше использовать такой формат:

```python
prompt = f"""
Ты помощник Faktura.uz.

Отвечай только используя контекст.

Если информации недостаточно,
честно сообщи об этом.

Язык ответа: {language}

Контекст:
{context}

Вопрос:
{question}
"""
```

---

# Проверка после замены

Создай файл:

## `test_qwen.py`

```python
from app.llm.ollama_client import ask_ollama

answer = ask_ollama(
    "Что такое электронный документооборот?"
)

print(answer)
```

Запуск:

```bash
python test_qwen.py
```

Если ответ появился — Ollama работает.

---

# Полная схема после перехода

```text
User
↓
Telegram
↓
handlers.py
↓
bot_engine.py
↓
query_rewriter.py
↓
retriever.py
↓
ChromaDB
↓
generator.py
↓
ollama_client.py
↓
Qwen3
↓
Ответ
```

---

Я бы рекомендовал ещё один шаг: **не удалять Gemini сразу**, а сделать переключатель.

Например:

```python
LLM_PROVIDER=gemini
```

или

```python
LLM_PROVIDER=ollama
```

Тогда можно будет одной строкой в `.env` переключаться между моделями и сравнивать качество ответов на одинаковых вопросах. Это очень удобно на этапе тестирования Faktura Assistant.








!!!!
не работает api
кнопка назад при английском и узбекском не работает
жирный текст
модель еще тупит
посчитать стоимость модели и сервера
админ панель для просмотра плохих ответов
авторизация процессов
фото запрос


