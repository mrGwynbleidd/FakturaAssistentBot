05-06-26
1. Собрать все PDF в data/pdf/.
2. Создать FAQ PDF по проблемам входа, пароля, ЭЦП и доступа.
3. Обновить prompt в generator.py.
4. Подключить query_rewriter.py в retriever.py.
5. Пересобрать индекс и протестировать 20 вопросов.


Отлично. Сейчас **не подключаем Telegram-группу**. Делаем сначала “мозг бота” локально:

```text
вопрос пользователя
   ↓
определение языка
   ↓
поиск по PDF/Chroma
   ↓
Gemini генерирует ответ
   ↓
сохранение вопроса и ответа в CSV
   ↓
ответ выводится в терминале
```

То есть мы создаём **ядро бота**, которое потом легко подключим к Telegram.

---

# Шаг 1. Текущая структура проекта

У тебя должно быть примерно так:

```text
faktura_bot/
│
├── app/
│   ├── bot/
│   │   └── language.py
│   │
│   ├── core/
│   │   └── bot_engine.py
│   │
│   ├── logs/
│   │   └── csv_logger.py
│   │
│   ├── rag/
│   │   ├── pdf_loader.py
│   │   ├── splitter.py
│   │   ├── embedding.py
│   │   ├── index_builder.py
│   │   ├── query_rewriter.py
│   │   ├── retriever.py
│   │   └── generator.py
│   │
│   └── config.py
│
├── data/
│   ├── pdf/
│   ├── chroma_db/
│   └── raw/
│       └── QA_outputs.csv
│
├── build_index.py
├── test_bot_engine.py
├── .env
└── requirements.txt
```

Теперь у нас есть нормальное ядро:

```text
test_bot_engine.py
        ↓
process_user_question()
        ↓
detect_language()
        ↓
retrieve_context()
        ↓
rewrite_query()
        ↓
Chroma search
        ↓
generate_answer()
        ↓
Gemini API
        ↓
save_qa()
        ↓
CSV log
```


вопрос пользователя
   ↓
бот не нашёл хороший ответ
   ↓
вопрос сохраняется как problem case
   ↓
админ потом пишет правильный ответ
   ↓
этот ответ сохраняется в базу обучения
   ↓
после подтверждения он добавляется в knowledge base
   ↓
пересоздаём embeddings
   ↓
в следующий раз бот уже отвечает лучше
```

PDF + approved cases → Chroma → Gemini → ответ
                         ↑
                         |
        user question + admin answer + review
```

### Do not save question in needs_review
add faktura API https://api.faktura.uz/docs/
Create tg-bot
take questions from needs_review auto, after review auto embedding 
interface for user and admin. Learn from real-user question and admins answer
https://t.me/fakturauz Assume, sometimes admins do not write it, sometimes very short
check cyrillic uzbek!!!
Photo embedding, understand photo and able to understand question from photo and mb little text from user 

###Need tg-bot that will kick and report mailing, ad bots, deleate process_user_question
Need dataset with keywords, and ability to detect it other languages


###learn git 


###agent, show cursor


!!!!!!!!!!!

Да, я посмотрел репозиторий. У тебя уже есть нужные папки `app/learning` и `app/faktura_api`; в `learning` лежат `approved_loader.py`, `case_logger.py`, `review_manager.py`, а в `faktura_api` есть `call_center_api.py` и другие API-файлы. ([GitHub][1]) ([GitHub][2])

Главный ответ: **реальные кейсы call center не нужно сразу писать в `approved.csv`**. Правильнее так:

```text
call_center_api.py
    ↓
data/learning/call_center_cases_raw.csv  ← сырые реальные кейсы
    ↓
ручная/AI-нормализация
    ↓
data/learning/approved.csv               ← только проверенные хорошие ответы
    ↓
build_index.py
    ↓
ChromaDB
```

`approved.csv` должен быть **чистой базой обучения**, потому что всё, что туда попадёт, потом индексируется и влияет на ответы бота.

---

# Что сейчас не так

## 1. `approved.csv` может ломаться из-за старого заголовка

В текущем `review_manager.py` путь идёт в:

```text
data/learning/approved.csv
```

и поля там такие: `case_id`, `datetime`, `language`, `question`, `approved_answer`, `category`, `source_type`. ([GitHub][3])

Но проверка заголовка слабая:

```python
has_header = "case_id" in first_line
```

Если старый CSV уже содержит `case_id`, но остальные столбцы неправильные, код будет продолжать писать в сломанный файл.

---

## 2. `call_center_api.py` сейчас сохраняет в отдельный файл, но не в правильной структуре

Сейчас там указан:

```python
OUTPUT_CSV = "daodesk_cases.csv"
```

и формируются только `ticket_id`, `question`, `answer`, `date`. ([GitHub][4])

Для обучения этого мало. Нужно добавить:

```text
case_id
language
question
approved_answer / admin_answer
category
source_type
source_id
status
```

---

## 3. В `index_builder.py` теряется metadata approved cases

Сейчас `index_builder.py` загружает approved cases, но при добавлении chunk metadata сохраняет только:

```python
{"source": source}
```

из-за этого теряются `source_type`, `category`, `language`. ([GitHub][5])

---

# Что нужно сделать

## Шаг 1. Замени `app/learning/review_manager.py`

Этот файл будет правильно сохранять approved cases и не ломать CSV.

```python
import csv
from pathlib import Path
from datetime import datetime


BASE_DIR = Path(__file__).resolve().parents[2]
APPROVED_CASES_PATH = BASE_DIR / "data" / "learning" / "approved.csv"


APPROVED_FIELDNAMES = [
    "case_id",
    "datetime",
    "language",
    "question",
    "approved_answer",
    "category",
    "source_type",
    "source_id",
    "status",
    "notes",
]


def clean_text(value: str | None) -> str:
    """
    Чистит текст для CSV:
    - убирает лишние переносы строк;
    - убирает лишние пробелы;
    - не ломает структуру CSV.
    """

    if not value:
        return ""

    return " ".join(str(value).split())


def ensure_csv_header(path: Path, fieldnames: list[str]) -> None:
    """
    Проверяет заголовок CSV.
    Если файл существует, но заголовок неправильный,
    делает backup и создаёт новый правильный файл.
    """

    path.parent.mkdir(parents=True, exist_ok=True)

    if not path.exists():
        return

    with open(path, mode="r", encoding="utf-8-sig", newline="") as file:
        reader = csv.reader(file)
        current_header = next(reader, [])

    if current_header != fieldnames:
        backup_path = path.with_name(
            f"{path.stem}_backup_{datetime.now().strftime('%Y%m%d_%H%M%S')}{path.suffix}"
        )
        path.rename(backup_path)
        print(f"Old approved.csv header was wrong. Backup created: {backup_path}")


def save_approved_case(
    case_id: str,
    question: str,
    approved_answer: str,
    language: str = "ru",
    category: str = "general",
    source_type: str = "admin_review",
    source_id: str = "",
    status: str = "approved",
    notes: str = "",
) -> None:
    """
    Сохраняет один проверенный кейс в approved.csv.

    Важно:
    approved.csv должен содержать только хорошие ответы,
    потому что этот файл индексируется в ChromaDB.
    """

    ensure_csv_header(APPROVED_CASES_PATH, APPROVED_FIELDNAMES)

    file_exists = APPROVED_CASES_PATH.exists()

    row = {
        "case_id": clean_text(case_id),
        "datetime": datetime.now().strftime("%Y-%m-%d %H:%M:%S"),
        "language": clean_text(language),
        "question": clean_text(question),
        "approved_answer": clean_text(approved_answer),
        "category": clean_text(category),
        "source_type": clean_text(source_type),
        "source_id": clean_text(source_id),
        "status": clean_text(status),
        "notes": clean_text(notes),
    }

    with open(APPROVED_CASES_PATH, mode="a", encoding="utf-8-sig", newline="") as file:
        writer = csv.DictWriter(
            file,
            fieldnames=APPROVED_FIELDNAMES,
            quoting=csv.QUOTE_ALL,
            extrasaction="ignore",
        )

        if not file_exists:
            writer.writeheader()

        writer.writerow(row)


def approve_case_manually(
    case_id: str,
    question: str,
    approved_answer: str,
    language: str = "ru",
    category: str = "general",
) -> None:
    """
    Старое имя функции оставляем, чтобы test_approve_case.py не сломался.
    """

    save_approved_case(
        case_id=case_id,
        question=question,
        approved_answer=approved_answer,
        language=language,
        category=category,
        source_type="admin_review",
        source_id=case_id,
        status="approved",
    )
```

---

## Шаг 2. Замени `app/learning/approved_loader.py`

Этот файл будет читать только approved cases со статусом `approved`.

```python
import csv
from pathlib import Path


BASE_DIR = Path(__file__).resolve().parents[2]
APPROVED_CASES_PATH = BASE_DIR / "data" / "learning" / "approved.csv"


def load_approved_cases() -> list[dict]:
    """
    Загружает approved cases из approved.csv
    и превращает их в документы для ChromaDB.
    """

    if not APPROVED_CASES_PATH.exists():
        return []

    documents = []

    with open(APPROVED_CASES_PATH, mode="r", encoding="utf-8-sig", newline="") as file:
        reader = csv.DictReader(file)

        for row in reader:
            status = row.get("status", "approved").strip().lower()

            if status and status != "approved":
                continue

            question = row.get("question", "").strip()
            answer = row.get("approved_answer", "").strip()
            language = row.get("language", "unknown").strip()
            category = row.get("category", "general").strip()
            case_id = row.get("case_id", "").strip()
            source_type = row.get("source_type", "admin_review").strip()
            source_id = row.get("source_id", case_id).strip()

            if not question or not answer:
                continue

            text = f"""
User question:
{question}

Correct support answer:
{answer}
""".strip()

            documents.append(
                {
                    "source": f"{source_type}:{source_id or case_id}",
                    "text": text,
                    "metadata": {
                        "source": f"{source_type}:{source_id or case_id}",
                        "case_id": case_id,
                        "language": language,
                        "category": category,
                        "source_type": source_type,
                        "source_id": source_id,
                    },
                }
            )

    return documents
```

---

## Шаг 3. Исправь `app/rag/index_builder.py`

Нужно сохранить metadata от approved cases, а не только `source`.

Найди в `index_builder.py` этот кусок:

```python
metadatas.append(
    {
        "source": source,
    }
)
```

Замени на:

```python
chunk_metadata = doc.get("metadata", {}).copy()
chunk_metadata["source"] = source

metadatas.append(chunk_metadata)
```

И ещё исправь вывод:

```python
print(f"Founded approved cases: {len(approved_cases)}")
```

---

# Шаг 4. Замени `app/faktura_api/call_center_api.py`

Этот файл должен сохранять **сырые кейсы** отдельно:

```text
data/learning/call_center_cases_raw.csv
```

Не в `approved.csv`.

```python
import csv
import time
from pathlib import Path

import requests
from requests.auth import HTTPBasicAuth
from bs4 import BeautifulSoup

from app.config import EMAIL, API_PARAM


BASE_DIR = Path(__file__).resolve().parents[2]

# В .env лучше добавить:
# CALL_CENTER_DOMAIN=https://your-domain.daodesk.com
CALL_CENTER_DOMAIN = "https://d"

CLOSED_STATUS = "closed,resolved"
REQUEST_PAUSE = 0.4

BASE_URL = f"{CALL_CENTER_DOMAIN.rstrip('/')}/api/v2"

OUTPUT_CSV = BASE_DIR / "data" / "learning" / "call_center_cases_raw.csv"

RAW_FIELDNAMES = [
    "case_id",
    "ticket_id",
    "datetime",
    "language",
    "question",
    "admin_answer",
    "category",
    "source_type",
    "source_id",
    "status",
]


auth = HTTPBasicAuth(EMAIL, API_PARAM)


def strip_html(html: str) -> str:
    """
    Убирает HTML-теги из сообщения.
    """

    return BeautifulSoup(html or "", "html.parser").get_text(
        separator=" ",
        strip=True,
    )


def clean_text(value: str | None) -> str:
    """
    Чистит текст для CSV.
    """

    if not value:
        return ""

    return " ".join(str(value).split())


def extract_items(payload) -> list[dict]:
    """
    Универсально достаёт список элементов из ответа API.

    Поддерживает:
    - list
    - {"data": [...]}
    - {"data": {...}}
    - {"items": [...]}
    """

    if isinstance(payload, list):
        return payload

    if not isinstance(payload, dict):
        return []

    data = payload.get("data")

    if isinstance(data, list):
        return data

    if isinstance(data, dict):
        return list(data.values())

    items = payload.get("items")

    if isinstance(items, list):
        return items

    return []


def get_tickets(page: int) -> dict:
    """
    Получает список тикетов.
    """

    params = {"page": page}

    if CLOSED_STATUS:
        params["status_list"] = CLOSED_STATUS

    response = requests.get(
        f"{BASE_URL}/tickets/",
        params=params,
        auth=auth,
        timeout=30,
    )

    response.raise_for_status()

    return response.json()


def get_posts(ticket_id: int) -> list[dict]:
    """
    Получает сообщения внутри тикета.
    """

    response = requests.get(
        f"{BASE_URL}/tickets/{ticket_id}/posts/",
        params={"page": 1},
        auth=auth,
        timeout=30,
    )

    response.raise_for_status()

    return extract_items(response.json())


def build_qa(ticket: dict) -> dict | None:
    """
    Собирает Q/A из одного тикета:
    - вопрос пользователя;
    - ответ сотрудников call center.
    """

    ticket_id = ticket.get("id")

    if not ticket_id:
        return None

    client_id = ticket.get("user_id")

    posts = get_posts(ticket_id)

    if not posts:
        return None

    posts.sort(key=lambda post: post.get("id", 0))

    questions = []
    answers = []

    for post in posts:
        text = strip_html(post.get("text", ""))

        if not text:
            continue

        if post.get("user_id") == client_id:
            questions.append(text)
        else:
            answers.append(text)

    question = clean_text(
        f"{ticket.get('title', '')} {' '.join(questions)}"
    )

    answer = clean_text(
        " ".join(answers)
    )

    if not question or not answer:
        return None

    return {
        "case_id": f"callcenter_{ticket_id}",
        "ticket_id": ticket_id,
        "datetime": ticket.get("date_created", ""),
        "language": "ru",
        "question": question,
        "admin_answer": answer,
        "category": "call_center",
        "source_type": "call_center",
        "source_id": str(ticket_id),
        "status": "raw",
    }


def save_call_center_cases(max_pages: int | None = None) -> None:
    """
    Сохраняет реальные кейсы call center в отдельный raw CSV.
    """

    OUTPUT_CSV.parent.mkdir(parents=True, exist_ok=True)

    rows = []
    page = 1

    while True:
        data = get_tickets(page)
        tickets = extract_items(data)

        if not tickets:
            break

        for ticket in tickets:
            qa = build_qa(ticket)

            if qa:
                rows.append(qa)
                print(
                    f"Ticket {qa['ticket_id']}: {qa['question'][:60]}..."
                )

            time.sleep(REQUEST_PAUSE)

        pagination = data.get("pagination", {}) if isinstance(data, dict) else {}
        total_pages = pagination.get("total_pages", page)

        print(f"Page {page}/{total_pages} processed")

        if max_pages and page >= max_pages:
            break

        if page >= total_pages:
            break

        page += 1
        time.sleep(REQUEST_PAUSE)

    with open(OUTPUT_CSV, mode="w", encoding="utf-8-sig", newline="") as file:
        writer = csv.DictWriter(
            file,
            fieldnames=RAW_FIELDNAMES,
            quoting=csv.QUOTE_ALL,
        )

        writer.writeheader()
        writer.writerows(rows)

    print(f"\nReady: {len(rows)} cases saved in {OUTPUT_CSV}")


if __name__ == "__main__":
    save_call_center_cases()
```

---

# Шаг 5. Добавь импорт raw cases в approved.csv

Создай новый файл:

```text
app/learning/import_call_center_to_approved.py
```

Код:

```python
import csv
from pathlib import Path

from app.learning.review_manager import save_approved_case


BASE_DIR = Path(__file__).resolve().parents[2]

RAW_CALL_CENTER_PATH = BASE_DIR / "data" / "learning" / "call_center_cases_raw.csv"


def import_call_center_to_approved(
    min_question_len: int = 10,
    min_answer_len: int = 30,
    auto_approve: bool = False,
) -> None:
    """
    Импортирует call center cases в approved.csv.

    Важно:
    auto_approve=False по умолчанию.
    Лучше сначала посмотреть raw cases вручную.
    """

    if not RAW_CALL_CENTER_PATH.exists():
        print(f"File not found: {RAW_CALL_CENTER_PATH}")
        return

    imported = 0
    skipped = 0

    with open(RAW_CALL_CENTER_PATH, mode="r", encoding="utf-8-sig", newline="") as file:
        reader = csv.DictReader(file)

        for row in reader:
            question = row.get("question", "").strip()
            answer = row.get("admin_answer", "").strip()
            ticket_id = row.get("ticket_id", "").strip()
            case_id = row.get("case_id", f"callcenter_{ticket_id}").strip()

            if len(question) < min_question_len or len(answer) < min_answer_len:
                skipped += 1
                continue

            if not auto_approve:
                skipped += 1
                continue

            save_approved_case(
                case_id=case_id,
                question=question,
                approved_answer=answer,
                language=row.get("language", "ru"),
                category=row.get("category", "call_center"),
                source_type="call_center",
                source_id=ticket_id,
                status="approved",
                notes="Imported from call center API.",
            )

            imported += 1

    print(f"Imported: {imported}")
    print(f"Skipped: {skipped}")


if __name__ == "__main__":
    # В первый раз лучше не автоаппрувить.
    # После проверки raw CSV можно поставить auto_approve=True.
    import_call_center_to_approved(auto_approve=False)
```

---

# Как использовать

## 1. Сначала собери реальные кейсы

```bash
python -m app.faktura_api.call_center_api
```

Появится:

```text
data/learning/call_center_cases_raw.csv
```

---

## 2. Открой raw CSV и проверь

Посмотри:

```text
question
admin_answer
```

Если ответы нормальные, можно импортировать.

---

## 3. Включи импорт в approved

В файле:

```text
app/learning/import_call_center_to_approved.py
```

внизу временно поменяй:

```python
import_call_center_to_approved(auto_approve=False)
```

на:

```python
import_call_center_to_approved(auto_approve=True)
```




Запусти:

```bash
python -m app.learning.import_call_center_to_approved
```

---

## 4. Пересобери Chroma

```bash
python build_index.py
```

Теперь approved call center cases попадут в базу знаний.

---

# Нужно ли сохранять call center ответы сразу в approved.csv?

**Нет, не сразу.**

Лучший вариант:

```text
call_center_cases_raw.csv  — всё как пришло из API
approved.csv               — только проверенные хорошие ответы
```

Почему:

```text
1. Ответы операторов могут быть короткие.
2. Там могут быть персональные данные.
3. Может быть мусор из HTML.
4. Может быть неполный ответ.
5. Если сразу отправить это в Chroma, бот начнёт учиться на плохих данных.
```

---

# Ещё важное

Сейчас в `test_approve_case.py` у тебя очень длинный ответ с кавычками, переносами и Markdown. Это нормально, но лучше после замены `review_manager.py` удалить старый `approved.csv`, потому что он уже мог быть создан с неправильными столбцами.

В PowerShell:

```powershell
Remove-Item data\learning\approved.csv -ErrorAction SilentlyContinue
```

Потом:

```bash
python test_approve_case.py
python build_index.py
```

---

# Итог

Тебе нужно сделать так:

```text
1. approved.csv — только чистые проверенные кейсы.
2. call_center_cases_raw.csv — сырые реальные обращения.
3. import_call_center_to_approved.py — переносит хорошие кейсы в approved.
4. build_index.py — индексирует PDF + approved cases.
```

Это даст нормальное “обучение” без загрязнения базы плохими ответами.

[1]: https://github.com/mrGwynbleidd/FakturaAssistentBot/tree/main/app/learning "FakturaAssistentBot/app/learning at main · mrGwynbleidd/FakturaAssistentBot · GitHub"
[2]: https://github.com/mrGwynbleidd/FakturaAssistentBot/tree/main/app/faktura_api "FakturaAssistentBot/app/faktura_api at main · mrGwynbleidd/FakturaAssistentBot · GitHub"
[3]: https://raw.githubusercontent.com/mrGwynbleidd/FakturaAssistentBot/main/app/learning/review_manager.py "raw.githubusercontent.com"
[4]: https://raw.githubusercontent.com/mrGwynbleidd/FakturaAssistentBot/main/app/faktura_api/call_center_api.py "raw.githubusercontent.com"
[5]: https://raw.githubusercontent.com/mrGwynbleidd/FakturaAssistentBot/main/app/rag/index_builder.py "raw.githubusercontent.com"




!!!!!!!

Да, это правильный следующий шаг. Сейчас админ-панель лучше перестроить так, чтобы она не была одним огромным `admin_handler.py`, а стала отдельным модулем с маленькими файлами по функциям.

Главная идея:

```text
admin panel
    ↓
создать постоянный Q/A
    ↓
создать временную проблему / инцидент
    ↓
активировать режим “отвечать так на все похожие вопросы”
    ↓
бот сначала проверяет active_incidents
    ↓
если совпало — отвечает готовым ответом
    ↓
если нет — идёт в Faktura API / Chroma / Qwen
```

---

# 1. Новая структура папок

Я бы сделал так:

```text
app/
├── admin/
│   ├── __init__.py
│   │
│   ├── admin_router.py
│   ├── admin_config.py
│   │
│   ├── texts/
│   │   ├── __init__.py
│   │   ├── ru.py
│   │   ├── uz.py
│   │   └── en.py
│   │
│   ├── keyboards/
│   │   ├── __init__.py
│   │   ├── main.py
│   │   ├── knowledge.py
│   │   ├── incidents.py
│   │   └── review.py
│   │
│   ├── states/
│   │   ├── __init__.py
│   │   ├── knowledge_states.py
│   │   ├── incident_states.py
│   │   └── review_states.py
│   │
│   ├── handlers/
│   │   ├── __init__.py
│   │   ├── main_menu.py
│   │   ├── knowledge_create.py
│   │   ├── incidents_create.py
│   │   ├── incidents_manage.py
│   │   ├── review_cases.py
│   │   └── stats.py
│   │
│   └── services/
│       ├── __init__.py
│       ├── knowledge_service.py
│       ├── incident_service.py
│       ├── review_service.py
│       └── admin_access.py
│
├── learning/
│   ├── approved_loader.py
│   ├── review_manager.py
│   ├── case_logger.py
│   └── import_call_center_to_approved.py
│
├── core/
│   ├── bot_engine.py
│   └── emergency_answer_checker.py
```

---

# 2. Что будет где лежать

## `admin_router.py`

Главный файл, который просто собирает все маленькие роутеры.

```python
from aiogram import Router

from app.admin.handlers.main_menu import router as main_menu_router
from app.admin.handlers.knowledge_create import router as knowledge_create_router
from app.admin.handlers.incidents_create import router as incidents_create_router
from app.admin.handlers.incidents_manage import router as incidents_manage_router
from app.admin.handlers.review_cases import router as review_cases_router
from app.admin.handlers.stats import router as stats_router


admin_router = Router()

admin_router.include_router(main_menu_router)
admin_router.include_router(knowledge_create_router)
admin_router.include_router(incidents_create_router)
admin_router.include_router(incidents_manage_router)
admin_router.include_router(review_cases_router)
admin_router.include_router(stats_router)
```

!!!!!!!!!
А в `app/main.py` будет:

```python
from app.bot.handlers import router as user_router
from app.admin.admin_router import admin_router

dp.include_router(admin_router)
dp.include_router(user_router)
```
!!!!!!!!!!!!1

---

# 3. Постоянные Q/A и временные проблемы нужно разделить

Это очень важно.

У тебя будут два разных типа знаний.

---

## Тип 1. Постоянные знания

Например:

```text
Вопрос:
Как создать ТТН?

Ответ:
Для создания ТТН перейдите...
```

Это можно сохранять в:

```text
data/learning/approved.csv
```

Потому что это стабильная база знаний.

---

## Тип 2. Временная проблема / инцидент

Например:

```text
Проблема:
Сбой в soliq.uz

Ответ:
Сейчас наблюдаются временные сбои на стороне soliq.uz. 
Рекомендуем повторить отправку позже.
```

Это **нельзя** сохранять только в `approved.csv`, потому что через день проблема закончится, а бот продолжит так отвечать.

Для этого нужен отдельный файл:

```text
data/admin/active_incidents.csv
```

или лучше:

```text
data/admin/incidents.csv
```

---

# 4. Файлы данных

Создай папку:

```text
data/admin/
```

Внутри:

```text
data/admin/
├── admin_knowledge.csv
├── incidents.csv
├── incident_logs.csv
└── admin_actions_log.csv
```

---

# 5. `admin_knowledge.csv`

Это ручные постоянные ответы от админа.

Колонки:

```csv
knowledge_id,datetime,language,question,answer,category,tags,status,created_by,notes
```

Пример:

```csv
knowledge_id,datetime,language,question,answer,category,tags,status,created_by,notes
kb_001,2026-06-15,ru,Как создать ТТН?,Для создания ТТН перейдите...,documents,ттн;накладная;создать,approved,123456789,
```

Потом эти записи можно импортировать в `approved.csv` или сразу индексировать вместе с approved cases.

---

# 6. `incidents.csv`

Это временные активные проблемы.

Колонки:

```csv
incident_id,datetime,language,title,problem_text,answer,keywords,match_mode,priority,status,start_at,end_at,created_by,notes
```

Пример:

```csv
incident_id,datetime,language,title,problem_text,answer,keywords,match_mode,priority,status,start_at,end_at,created_by,notes
inc_001,2026-06-15,ru,Сбой Soliq,Не работает отправка в soliq,Сейчас наблюдаются временные сбои на стороне soliq.uz. Повторите отправку позже.,soliq;отправка;ошибка;сервер,contains,100,active,2026-06-15 09:00,2026-06-15 18:00,123456789,
```

---

# 7. Функция “отвечать так на все вопросы по этой проблеме”

Для этого нужен отдельный файл:

```text
app/core/emergency_answer_checker.py
```

Он будет работать **до Chroma, до API и до Qwen**.

Схема:

```text
Вопрос пользователя
    ↓
Проверить active incidents
    ↓
Если совпали keywords
    ↓
Сразу вернуть готовый ответ
    ↓
Не вызывать Qwen / Gemini
```

То есть в `bot_engine.py` порядок должен быть такой:

```python
def process_user_question(question: str, ...):
    language = detect_language(question)

    emergency_answer = check_active_incidents(question, language)

    if emergency_answer:
        return {
            "answer": emergency_answer,
            "sources": [
                {
                    "source": "Admin active incident",
                    "source_type": "incident",
                }
            ],
            "sent_to_review": False,
        }

    # дальше уже обычный путь:
    # 1. Faktura API
    # 2. ChromaDB
    # 3. Qwen/Gemini
```

Это решит твой кейс:

```text
Админ:
“Сейчас сбой в Soliq. Всем, кто спрашивает про отправку/ошибку/soliq, отвечать вот так...”

Пользователь:
“Почему не отправляется счет-фактура?”

Бот:
“Сейчас наблюдаются временные сбои на стороне soliq.uz...”
```

---

# 8. Какие кнопки должны быть в админ-панели

Главное меню админа:

```text
➕ Добавить Q/A
🚨 Добавить временную проблему
📋 Активные проблемы
✅ Review cases
📊 Статистика
⚙️ Настройки
⬅️ Выйти
```

---

# 9. Раздел “Добавить Q/A”

Логика:

```text
Админ нажимает:
➕ Добавить Q/A

Бот:
Введите вопрос

Админ:
Как создать ТТН?

Бот:
Введите правильный ответ

Админ:
Для создания ТТН перейдите...

Бот:
Выберите тип:
1. Постоянное знание
2. Временная проблема

Админ:
Постоянное знание

Бот:
Сохранено.
```

Сохраняем в:

```text
data/admin/admin_knowledge.csv
```

Или сразу в:

```text
data/learning/approved.csv
```

Моя рекомендация: сначала `admin_knowledge.csv`, потом отдельной функцией импортировать в `approved.csv`.

---

# 10. Раздел “Добавить временную проблему”

Логика:

```text
Админ нажимает:
🚨 Добавить временную проблему

Бот:
Введите название проблемы

Админ:
Сбой Soliq

Бот:
Введите ключевые слова через запятую

Админ:
soliq, ошибка, отправка, счет-фактура, сервер

Бот:
Введите ответ, который нужно показывать пользователям

Админ:
Сейчас наблюдаются временные сбои...

Бот:
До какого времени проблема активна?

Админ:
Сегодня до 18:00

Бот:
Проблема активирована.
```

Сохраняем в:

```text
data/admin/incidents.csv
```

---

# 11. Раздел “Активные проблемы”

Кнопки:

```text
📋 Активные проблемы
```

Бот показывает:

```text
1. Сбой Soliq
Статус: active
Ключевые слова: soliq, ошибка, отправка
До: 18:00

2. Техосмотр Faktura
Статус: active
Ключевые слова: вход, кабинет, недоступен
До: 23:00
```

Под каждой проблемой:

```text
🔴 Отключить
✏️ Изменить ответ
⏰ Продлить
🗑 Удалить
```

---

# 12. Где хранить тексты

Чтобы потом легко перевести кнопки и сообщения, нужно убрать все тексты из handlers.

Не так:

```python
await message.answer("Введите вопрос")
```

А так:

```python
await message.answer(ADMIN_TEXTS[language]["enter_question"])
```

---

Структура:

```text
app/admin/texts/
├── ru.py
├── uz.py
└── en.py
```

---

## `app/admin/texts/ru.py`

```python
ADMIN_TEXTS_RU = {
    "admin_menu_title": "Админ-панель",
    "add_qa": "➕ Добавить Q/A",
    "add_incident": "🚨 Добавить временную проблему",
    "active_incidents": "📋 Активные проблемы",
    "review_cases": "✅ Review cases",
    "stats": "📊 Статистика",
    "settings": "⚙️ Настройки",
    "exit": "⬅️ Выйти",

    "enter_question": "Введите вопрос:",
    "enter_answer": "Введите правильный ответ:",
    "enter_incident_title": "Введите название проблемы:",
    "enter_incident_keywords": "Введите ключевые слова через запятую:",
    "enter_incident_answer": "Введите ответ, который бот должен показывать пользователям:",
    "enter_incident_end_time": "До какого времени проблема активна?",

    "saved_successfully": "Сохранено успешно.",
    "incident_activated": "Временная проблема активирована.",
    "incident_disabled": "Проблема отключена.",
    "cancel": "Отмена",
    "back": "Назад",
}
```

---

## `app/admin/texts/__init__.py`

```python
from app.admin.texts.ru import ADMIN_TEXTS_RU
from app.admin.texts.uz import ADMIN_TEXTS_UZ
from app.admin.texts.en import ADMIN_TEXTS_EN


def get_admin_text(key: str, language: str = "ru") -> str:
    texts = {
        "ru": ADMIN_TEXTS_RU,
        "uz": ADMIN_TEXTS_UZ,
        "en": ADMIN_TEXTS_EN,
    }

    return texts.get(language, ADMIN_TEXTS_RU).get(key, key)
```

---

# 13. Где хранить кнопки

```text
app/admin/keyboards/
├── main.py
├── knowledge.py
├── incidents.py
└── review.py
```

---

## `main.py`

```python
from aiogram.types import ReplyKeyboardMarkup, KeyboardButton

from app.admin.texts import get_admin_text


def admin_main_keyboard(language: str = "ru") -> ReplyKeyboardMarkup:
    keyboard = [
        [
            KeyboardButton(text=get_admin_text("add_qa", language)),
            KeyboardButton(text=get_admin_text("add_incident", language)),
        ],
        [
            KeyboardButton(text=get_admin_text("active_incidents", language)),
            KeyboardButton(text=get_admin_text("review_cases", language)),
        ],
        [
            KeyboardButton(text=get_admin_text("stats", language)),
            KeyboardButton(text=get_admin_text("settings", language)),
        ],
        [
            KeyboardButton(text=get_admin_text("exit", language)),
        ],
    ]

    return ReplyKeyboardMarkup(
        keyboard=keyboard,
        resize_keyboard=True,
    )
```

---

# 14. FSM states отдельно

Чтобы `admin_handler.py` не разрастался, состояния тоже нужно вынести.

```text
app/admin/states/
├── knowledge_states.py
├── incident_states.py
└── review_states.py
```

---

## `knowledge_states.py`

```python
from aiogram.fsm.state import StatesGroup, State


class AddKnowledgeStates(StatesGroup):
    waiting_for_question = State()
    waiting_for_answer = State()
    waiting_for_category = State()
    waiting_for_confirmation = State()
```

---

## `incident_states.py`

```python
from aiogram.fsm.state import StatesGroup, State


class AddIncidentStates(StatesGroup):
    waiting_for_title = State()
    waiting_for_keywords = State()
    waiting_for_answer = State()
    waiting_for_end_time = State()
    waiting_for_confirmation = State()
```

---

# 15. Services отдельно от handlers

Handlers должны только принимать сообщения.

Плохой вариант:

```python
@router.message(...)
async def add_incident(message):
    open csv
    write row
    validate
    format
    save
```

Хороший вариант:

```python
@router.message(...)
async def add_incident(message):
    incident_service.save_incident(...)
```

---

Сервисы:

```text
app/admin/services/
├── knowledge_service.py
├── incident_service.py
├── review_service.py
└── admin_access.py
```

---

## `knowledge_service.py`

Функции:

```python
save_admin_knowledge(...)
list_admin_knowledge(...)
delete_admin_knowledge(...)
export_knowledge_to_approved(...)
```

---

## `incident_service.py`

Функции:

```python
save_incident(...)
list_active_incidents(...)
disable_incident(...)
update_incident_answer(...)
extend_incident(...)
find_matching_incident(...)
```

---

## `admin_access.py`

Функции:

```python
is_admin(user_id: int) -> bool
get_admin_language(user_id: int) -> str
```

---

# 16. Как это будет работать в bot_engine

Итоговая логика ответа бота должна быть такой:

```text
1. Пользователь задал вопрос
2. Проверяем активные проблемы
3. Если проблема найдена — отвечаем готовым admin answer
4. Если нет — проверяем Faktura API
5. Если API не нужен — ищем в ChromaDB
6. Генерируем ответ через Qwen/Gemini
7. Логируем вопрос и ответ
8. Если ответ слабый — отправляем в review
```

---

# 17. Финальная архитектура знаний

В итоге у тебя будет 4 уровня знаний:

```text
1. active incidents
   Срочные временные ответы.
   Проверяются первыми.

2. Faktura API
   Реальные данные из API.

3. approved.csv
   Проверенные ответы операторов и админов.

4. PDF база
   Официальные инструкции Faktura Wiki.
```

Порядок приоритета:

```text
active incidents
    ↓
Faktura API
    ↓
approved cases
    ↓
PDF knowledge base
    ↓
Qwen/Gemini
```

---

# 18. Что делать сейчас

Сейчас не нужно сразу писать огромный код. Сначала создай структуру:

```text
app/admin/
├── admin_router.py
├── admin_config.py
├── texts/
├── keyboards/
├── states/
├── handlers/
└── services/
```

И данные:

```text
data/admin/
├── admin_knowledge.csv
├── incidents.csv
├── incident_logs.csv
└── admin_actions_log.csv
```

После этого можно спокойно переносить функции из большого `admin_handler.py` по частям:

```text
admin_handler.py
    ↓
main_menu.py
knowledge_create.py
incidents_create.py
incidents_manage.py
review_cases.py
stats.py
```

Так проект станет нормальным, расширяемым и готовым к переводу интерфейса.










!!!!!!!!!!

Да, папка `bot/` **нужна**. Её не удаляем.

Сейчас логика такая:

```text
app/bot/
= обычный пользовательский Telegram-бот

app/admin/
= отдельная админ-панель
```

То есть:

```text
bot/handlers.py
```

отвечает за обычных пользователей, вопросы, фото, меню, язык.

А новая папка:

```text
admin/
```

будет отвечать только за админов: review cases, добавление Q/A, временные проблемы, статистика, управление админами. В твоём репозитории сейчас уже есть папка `app/bot`, а админка пока находится внутри большого `admin_handlers.py`, поэтому мы аккуратно выносим её в отдельный модуль. ([GitHub][1])

Я разобью коды на **3 части**, чтобы не смешивать всё в один огромный ответ.

---

# Часть 1 из 3

В этой части будут базовые файлы для новой структуры:

```text
app/admin/
├── __init__.py
├── admin_config.py
│
├── texts/
│   ├── __init__.py
│   ├── ru.py
│   ├── uz.py
│   └── en.py
│
├── keyboards/
│   ├── __init__.py
│   ├── main.py
│   ├── knowledge.py
│   ├── incidents.py
│   └── review.py
│
└── states/
    ├── __init__.py
    ├── knowledge_states.py
    ├── incident_states.py
    └── review_states.py
```

Пока это **структура, тексты, кнопки и состояния**. Во 2-й части дам `services/`, в 3-й части — `handlers/`, `admin_router.py` и подключение к `main.py`.

---

# 1. Создай папки

В терминале из корня проекта:

```powershell
mkdir app\admin
mkdir app\admin\texts
mkdir app\admin\keyboards
mkdir app\admin\states
mkdir app\admin\handlers
mkdir app\admin\services
```

---

# 2. `app/admin/__init__.py`

```python
"""
Admin panel package.

Здесь будет новая модульная админ-панель:
- texts: все тексты и кнопки
- keyboards: клавиатуры
- states: FSM-состояния
- services: работа с CSV/логикой
- handlers: Telegram handlers
"""
```

---

# 3. `app/admin/admin_config.py`

```python
from app.config import ADMIN_IDS


def normalize_admin_ids(raw_admin_ids) -> set[int]:
    """
    Приводит ADMIN_IDS к set[int].

    Поддерживает варианты:
    - [123, 456]
    - "123,456"
    - ""
    - None
    """

    if not raw_admin_ids:
        return set()

    if isinstance(raw_admin_ids, set):
        return {int(admin_id) for admin_id in raw_admin_ids}

    if isinstance(raw_admin_ids, list):
        return {int(admin_id) for admin_id in raw_admin_ids}

    if isinstance(raw_admin_ids, tuple):
        return {int(admin_id) for admin_id in raw_admin_ids}

    if isinstance(raw_admin_ids, str):
        result = set()

        for item in raw_admin_ids.split(","):
            item = item.strip()

            if item.isdigit():
                result.add(int(item))

        return result

    return set()


ADMIN_ID_SET = normalize_admin_ids(ADMIN_IDS)


def is_admin_user(user_id: int | None) -> bool:
    """
    Проверяет, является ли пользователь админом.
    """

    if user_id is None:
        return False

    return int(user_id) in ADMIN_ID_SET
```

---

# 4. `app/admin/texts/ru.py`

```python
ADMIN_TEXTS_RU = {
    # Main menu
    "admin_menu_title": "Админ-панель",
    "admin_menu_description": "Выберите действие:",
    "access_denied": "У вас нет доступа к админ-панели.",
    "unknown_command": "Неизвестная команда админ-панели.",

    # Main buttons
    "btn_add_qa": "➕ Добавить Q/A",
    "btn_add_incident": "🚨 Добавить временную проблему",
    "btn_active_incidents": "📋 Активные проблемы",
    "btn_review_cases": "✅ Review cases",
    "btn_stats": "📊 Статистика",
    "btn_settings": "⚙️ Настройки",
    "btn_cancel": "❌ Отмена",
    "btn_back": "⬅️ Назад",
    "btn_exit": "⬅️ Выйти",

    # Confirmation buttons
    "btn_confirm": "✅ Подтвердить",
    "btn_reject": "❌ Отклонить",
    "btn_skip": "⏭ Пропустить",

    # Add Q/A
    "add_qa_start": "Создание нового Q/A.",
    "enter_question": "Введите вопрос, который может задать пользователь:",
    "enter_answer": "Введите правильный ответ на этот вопрос:",
    "enter_category": "Введите категорию. Например: login, documents, soliq, api, general.",
    "enter_tags": "Введите ключевые слова через запятую. Например: ттн, накладная, документ.",
    "qa_preview_title": "Проверьте Q/A перед сохранением:",
    "qa_saved": "Q/A успешно сохранён.",
    "qa_cancelled": "Создание Q/A отменено.",

    # Incidents
    "add_incident_start": "Создание временной проблемы.",
    "enter_incident_title": "Введите название проблемы. Например: Сбой Soliq.",
    "enter_incident_problem": "Кратко опишите проблему:",
    "enter_incident_keywords": "Введите ключевые слова через запятую. Например: soliq, ошибка, отправка.",
    "enter_incident_answer": "Введите ответ, который бот должен показывать пользователям:",
    "enter_incident_end_time": "Введите дату и время окончания активности. Например: 2026-06-16 18:00. Если срок неизвестен, напишите: none.",
    "incident_preview_title": "Проверьте временную проблему перед активацией:",
    "incident_saved": "Временная проблема сохранена и активирована.",
    "incident_cancelled": "Создание временной проблемы отменено.",
    "no_active_incidents": "Активных временных проблем нет.",
    "active_incidents_title": "Активные временные проблемы:",
    "incident_disabled": "Временная проблема отключена.",
    "incident_not_found": "Временная проблема не найдена.",

    # Review
    "review_cases_title": "Кейсы на проверке:",
    "no_review_cases": "Сейчас нет кейсов на проверке.",
    "enter_review_answer": "Введите правильный ответ для этого кейса:",
    "review_case_saved": "Кейс сохранён в approved.csv.",
    "review_case_rejected": "Кейс отклонён.",
    "review_case_not_found": "Кейс не найден.",

    # Stats
    "stats_title": "Статистика бота",
    "stats_empty": "Пока нет данных для статистики.",

    # Common
    "saved_successfully": "Сохранено успешно.",
    "operation_cancelled": "Операция отменена.",
    "error_occurred": "Произошла ошибка. Проверьте логи.",
}
```

---

# 5. `app/admin/texts/uz.py`

Пока можно оставить узбекский текст частично русским, чтобы потом спокойно перевести. Главное — структура уже готова.

```python
ADMIN_TEXTS_UZ = {
    # Main menu
    "admin_menu_title": "Admin panel",
    "admin_menu_description": "Amalni tanlang:",
    "access_denied": "Sizda admin panelga kirish huquqi yo‘q.",
    "unknown_command": "Noma’lum admin buyrug‘i.",

    # Main buttons
    "btn_add_qa": "➕ Q/A qo‘shish",
    "btn_add_incident": "🚨 Vaqtinchalik muammo qo‘shish",
    "btn_active_incidents": "📋 Faol muammolar",
    "btn_review_cases": "✅ Review cases",
    "btn_stats": "📊 Statistika",
    "btn_settings": "⚙️ Sozlamalar",
    "btn_cancel": "❌ Bekor qilish",
    "btn_back": "⬅️ Orqaga",
    "btn_exit": "⬅️ Chiqish",

    # Confirmation buttons
    "btn_confirm": "✅ Tasdiqlash",
    "btn_reject": "❌ Rad etish",
    "btn_skip": "⏭ O‘tkazib yuborish",

    # Add Q/A
    "add_qa_start": "Yangi Q/A yaratish.",
    "enter_question": "Foydalanuvchi berishi mumkin bo‘lgan savolni kiriting:",
    "enter_answer": "Ushbu savolga to‘g‘ri javobni kiriting:",
    "enter_category": "Kategoriyani kiriting. Masalan: login, documents, soliq, api, general.",
    "enter_tags": "Kalit so‘zlarni vergul orqali kiriting. Masalan: ttn, hujjat, soliq.",
    "qa_preview_title": "Saqlashdan oldin Q/A ni tekshiring:",
    "qa_saved": "Q/A muvaffaqiyatli saqlandi.",
    "qa_cancelled": "Q/A yaratish bekor qilindi.",

    # Incidents
    "add_incident_start": "Vaqtinchalik muammo yaratish.",
    "enter_incident_title": "Muammo nomini kiriting. Masalan: Soliq tizimida nosozlik.",
    "enter_incident_problem": "Muammoni qisqacha tasvirlab bering:",
    "enter_incident_keywords": "Kalit so‘zlarni vergul orqali kiriting. Masalan: soliq, xatolik, yuborish.",
    "enter_incident_answer": "Bot foydalanuvchilarga ko‘rsatishi kerak bo‘lgan javobni kiriting:",
    "enter_incident_end_time": "Faollik tugash vaqtini kiriting. Masalan: 2026-06-16 18:00. Agar noma’lum bo‘lsa: none.",
    "incident_preview_title": "Faollashtirishdan oldin vaqtinchalik muammoni tekshiring:",
    "incident_saved": "Vaqtinchalik muammo saqlandi va faollashtirildi.",
    "incident_cancelled": "Vaqtinchalik muammo yaratish bekor qilindi.",
    "no_active_incidents": "Faol vaqtinchalik muammolar yo‘q.",
    "active_incidents_title": "Faol vaqtinchalik muammolar:",
    "incident_disabled": "Vaqtinchalik muammo o‘chirildi.",
    "incident_not_found": "Vaqtinchalik muammo topilmadi.",

    # Review
    "review_cases_title": "Tekshiruvdagi keyslar:",
    "no_review_cases": "Hozircha tekshiruvdagi keyslar yo‘q.",
    "enter_review_answer": "Ushbu keys uchun to‘g‘ri javobni kiriting:",
    "review_case_saved": "Keys approved.csv fayliga saqlandi.",
    "review_case_rejected": "Keys rad etildi.",
    "review_case_not_found": "Keys topilmadi.",

    # Stats
    "stats_title": "Bot statistikasi",
    "stats_empty": "Hozircha statistika uchun ma’lumot yo‘q.",

    # Common
    "saved_successfully": "Muvaffaqiyatli saqlandi.",
    "operation_cancelled": "Amal bekor qilindi.",
    "error_occurred": "Xatolik yuz berdi. Loglarni tekshiring.",
}
```

---

# 6. `app/admin/texts/en.py`

```python
ADMIN_TEXTS_EN = {
    # Main menu
    "admin_menu_title": "Admin panel",
    "admin_menu_description": "Choose an action:",
    "access_denied": "You do not have access to the admin panel.",
    "unknown_command": "Unknown admin command.",

    # Main buttons
    "btn_add_qa": "➕ Add Q/A",
    "btn_add_incident": "🚨 Add temporary issue",
    "btn_active_incidents": "📋 Active issues",
    "btn_review_cases": "✅ Review cases",
    "btn_stats": "📊 Statistics",
    "btn_settings": "⚙️ Settings",
    "btn_cancel": "❌ Cancel",
    "btn_back": "⬅️ Back",
    "btn_exit": "⬅️ Exit",

    # Confirmation buttons
    "btn_confirm": "✅ Confirm",
    "btn_reject": "❌ Reject",
    "btn_skip": "⏭ Skip",

    # Add Q/A
    "add_qa_start": "Creating a new Q/A.",
    "enter_question": "Enter the question a user may ask:",
    "enter_answer": "Enter the correct answer for this question:",
    "enter_category": "Enter a category. Example: login, documents, soliq, api, general.",
    "enter_tags": "Enter keywords separated by commas. Example: ttn, invoice, document.",
    "qa_preview_title": "Review the Q/A before saving:",
    "qa_saved": "Q/A saved successfully.",
    "qa_cancelled": "Q/A creation cancelled.",

    # Incidents
    "add_incident_start": "Creating a temporary issue.",
    "enter_incident_title": "Enter the issue title. Example: Soliq outage.",
    "enter_incident_problem": "Briefly describe the issue:",
    "enter_incident_keywords": "Enter keywords separated by commas. Example: soliq, error, sending.",
    "enter_incident_answer": "Enter the answer the bot should show to users:",
    "enter_incident_end_time": "Enter the end time. Example: 2026-06-16 18:00. If unknown, write: none.",
    "incident_preview_title": "Review the temporary issue before activation:",
    "incident_saved": "Temporary issue saved and activated.",
    "incident_cancelled": "Temporary issue creation cancelled.",
    "no_active_incidents": "There are no active temporary issues.",
    "active_incidents_title": "Active temporary issues:",
    "incident_disabled": "Temporary issue disabled.",
    "incident_not_found": "Temporary issue not found.",

    # Review
    "review_cases_title": "Cases pending review:",
    "no_review_cases": "There are no cases pending review.",
    "enter_review_answer": "Enter the correct answer for this case:",
    "review_case_saved": "Case saved to approved.csv.",
    "review_case_rejected": "Case rejected.",
    "review_case_not_found": "Case not found.",

    # Stats
    "stats_title": "Bot statistics",
    "stats_empty": "No statistics data yet.",

    # Common
    "saved_successfully": "Saved successfully.",
    "operation_cancelled": "Operation cancelled.",
    "error_occurred": "An error occurred. Check the logs.",
}
```

---

# 7. `app/admin/texts/__init__.py`

```python
from app.admin.texts.ru import ADMIN_TEXTS_RU
from app.admin.texts.uz import ADMIN_TEXTS_UZ
from app.admin.texts.en import ADMIN_TEXTS_EN


ADMIN_TEXTS = {
    "ru": ADMIN_TEXTS_RU,
    "uz": ADMIN_TEXTS_UZ,
    "en": ADMIN_TEXTS_EN,
}


def get_admin_text(key: str, language: str = "ru") -> str:
    """
    Возвращает текст админ-панели по ключу.

    Если язык или ключ не найден, возвращает русский текст.
    Если ключа нет даже в русском, возвращает сам ключ.
    """

    language_texts = ADMIN_TEXTS.get(language, ADMIN_TEXTS_RU)

    return language_texts.get(
        key,
        ADMIN_TEXTS_RU.get(key, key),
    )
```

---

# 8. `app/admin/keyboards/__init__.py`

```python
"""
Admin keyboards package.
"""
```

---

# 9. `app/admin/keyboards/main.py`

```python
from aiogram.types import ReplyKeyboardMarkup, KeyboardButton

from app.admin.texts import get_admin_text


def admin_main_keyboard(language: str = "ru") -> ReplyKeyboardMarkup:
    """
    Главное меню админ-панели.
    """

    keyboard = [
        [
            KeyboardButton(text=get_admin_text("btn_add_qa", language)),
            KeyboardButton(text=get_admin_text("btn_add_incident", language)),
        ],
        [
            KeyboardButton(text=get_admin_text("btn_active_incidents", language)),
            KeyboardButton(text=get_admin_text("btn_review_cases", language)),
        ],
        [
            KeyboardButton(text=get_admin_text("btn_stats", language)),
            KeyboardButton(text=get_admin_text("btn_settings", language)),
        ],
        [
            KeyboardButton(text=get_admin_text("btn_exit", language)),
        ],
    ]

    return ReplyKeyboardMarkup(
        keyboard=keyboard,
        resize_keyboard=True,
    )


def admin_cancel_keyboard(language: str = "ru") -> ReplyKeyboardMarkup:
    """
    Клавиатура отмены операции.
    """

    keyboard = [
        [
            KeyboardButton(text=get_admin_text("btn_cancel", language)),
        ]
    ]

    return ReplyKeyboardMarkup(
        keyboard=keyboard,
        resize_keyboard=True,
    )


def admin_confirm_keyboard(language: str = "ru") -> ReplyKeyboardMarkup:
    """
    Клавиатура подтверждения.
    """

    keyboard = [
        [
            KeyboardButton(text=get_admin_text("btn_confirm", language)),
            KeyboardButton(text=get_admin_text("btn_cancel", language)),
        ]
    ]

    return ReplyKeyboardMarkup(
        keyboard=keyboard,
        resize_keyboard=True,
    )
```

---

# 10. `app/admin/keyboards/knowledge.py`

```python
from aiogram.types import ReplyKeyboardMarkup, KeyboardButton

from app.admin.texts import get_admin_text


def knowledge_category_keyboard(language: str = "ru") -> ReplyKeyboardMarkup:
    """
    Категории для ручного Q/A.
    """

    keyboard = [
        [
            KeyboardButton(text="login"),
            KeyboardButton(text="documents"),
        ],
        [
            KeyboardButton(text="soliq"),
            KeyboardButton(text="api"),
        ],
        [
            KeyboardButton(text="general"),
            KeyboardButton(text=get_admin_text("btn_cancel", language)),
        ],
    ]

    return ReplyKeyboardMarkup(
        keyboard=keyboard,
        resize_keyboard=True,
    )


def knowledge_after_save_keyboard(language: str = "ru") -> ReplyKeyboardMarkup:
    """
    Клавиатура после сохранения Q/A.
    """

    keyboard = [
        [
            KeyboardButton(text=get_admin_text("btn_add_qa", language)),
            KeyboardButton(text=get_admin_text("btn_back", language)),
        ]
    ]

    return ReplyKeyboardMarkup(
        keyboard=keyboard,
        resize_keyboard=True,
    )
```

---

# 11. `app/admin/keyboards/incidents.py`

```python
from aiogram.types import ReplyKeyboardMarkup, KeyboardButton

from app.admin.texts import get_admin_text


def incident_match_mode_keyboard(language: str = "ru") -> ReplyKeyboardMarkup:
    """
    Режим совпадения временной проблемы.

    contains:
    если вопрос содержит одно из ключевых слов.

    all_keywords:
    если вопрос содержит все ключевые слова.
    """

    keyboard = [
        [
            KeyboardButton(text="contains"),
            KeyboardButton(text="all_keywords"),
        ],
        [
            KeyboardButton(text=get_admin_text("btn_cancel", language)),
        ],
    ]

    return ReplyKeyboardMarkup(
        keyboard=keyboard,
        resize_keyboard=True,
    )


def incident_manage_keyboard(language: str = "ru") -> ReplyKeyboardMarkup:
    """
    Управление временными проблемами.
    """

    keyboard = [
        [
            KeyboardButton(text=get_admin_text("btn_active_incidents", language)),
            KeyboardButton(text=get_admin_text("btn_add_incident", language)),
        ],
        [
            KeyboardButton(text=get_admin_text("btn_back", language)),
        ],
    ]

    return ReplyKeyboardMarkup(
        keyboard=keyboard,
        resize_keyboard=True,
    )


def incident_after_save_keyboard(language: str = "ru") -> ReplyKeyboardMarkup:
    """
    Клавиатура после сохранения временной проблемы.
    """

    keyboard = [
        [
            KeyboardButton(text=get_admin_text("btn_add_incident", language)),
            KeyboardButton(text=get_admin_text("btn_active_incidents", language)),
        ],
        [
            KeyboardButton(text=get_admin_text("btn_back", language)),
        ],
    ]

    return ReplyKeyboardMarkup(
        keyboard=keyboard,
        resize_keyboard=True,
    )
```

---

# 12. `app/admin/keyboards/review.py`

```python
from aiogram.types import ReplyKeyboardMarkup, KeyboardButton

from app.admin.texts import get_admin_text


def review_action_keyboard(language: str = "ru") -> ReplyKeyboardMarkup:
    """
    Клавиатура для review case.
    """

    keyboard = [
        [
            KeyboardButton(text=get_admin_text("btn_confirm", language)),
            KeyboardButton(text=get_admin_text("btn_reject", language)),
        ],
        [
            KeyboardButton(text=get_admin_text("btn_skip", language)),
            KeyboardButton(text=get_admin_text("btn_cancel", language)),
        ],
    ]

    return ReplyKeyboardMarkup(
        keyboard=keyboard,
        resize_keyboard=True,
    )


def review_after_action_keyboard(language: str = "ru") -> ReplyKeyboardMarkup:
    """
    Клавиатура после review-действия.
    """

    keyboard = [
        [
            KeyboardButton(text=get_admin_text("btn_review_cases", language)),
            KeyboardButton(text=get_admin_text("btn_back", language)),
        ]
    ]

    return ReplyKeyboardMarkup(
        keyboard=keyboard,
        resize_keyboard=True,
    )
```

---

# 13. `app/admin/states/__init__.py`

```python
"""
Admin FSM states package.
"""
```

---

# 14. `app/admin/states/knowledge_states.py`

```python
from aiogram.fsm.state import StatesGroup, State


class AddKnowledgeStates(StatesGroup):
    """
    FSM для создания постоянного Q/A.
    """

    waiting_for_question = State()
    waiting_for_answer = State()
    waiting_for_category = State()
    waiting_for_tags = State()
    waiting_for_confirmation = State()
```

---

# 15. `app/admin/states/incident_states.py`

```python
from aiogram.fsm.state import StatesGroup, State


class AddIncidentStates(StatesGroup):
    """
    FSM для создания временной проблемы.

    Пример:
    - сбой Soliq
    - техосмотр Faktura
    - временная проблема с Google
    - проблема с отправкой документов
    """

    waiting_for_title = State()
    waiting_for_problem_text = State()
    waiting_for_keywords = State()
    waiting_for_answer = State()
    waiting_for_end_time = State()
    waiting_for_match_mode = State()
    waiting_for_confirmation = State()
```

---

# 16. `app/admin/states/review_states.py`

```python
from aiogram.fsm.state import StatesGroup, State


class ReviewCaseStates(StatesGroup):
    """
    FSM для проверки плохих ответов бота.
    """

    waiting_for_case_id = State()
    waiting_for_admin_answer = State()
    waiting_for_confirmation = State()
```

---

# Важно после этой части

Пока **не подключай `app/admin/admin_router.py` в `main.py`**, потому что handlers мы ещё не создали.

Сейчас ты можешь просто создать эти файлы и сохранить. Они не должны ломать проект.

---

# Что будет в части 2

В следующей части дам полные коды для:

```text
app/admin/services/
├── __init__.py
├── admin_access.py
├── knowledge_service.py
├── incident_service.py
├── review_service.py
└── stats_service.py
```

Там будет логика сохранения:

```text
data/admin/admin_knowledge.csv
data/admin/incidents.csv
data/admin/admin_actions_log.csv
```

И функции:

```text
save_admin_knowledge()
save_incident()
list_active_incidents()
find_matching_incident()
disable_incident()
get_review_cases()
get_bot_stats()
```

[1]: https://github.com/mrGwynbleidd/FakturaAssistentBot/tree/main/app "FakturaAssistentBot/app at main · mrGwynbleidd/FakturaAssistentBot · GitHub"



Ошибка именно из-за ADMIN_IDS.
У тебя где-то сейчас логика такая:
return user_id in ADMIN_IDS
А user_id — это int, например:
123456789
а ADMIN_IDS — строка из .env, например:
"123456789,987654321"
Поэтому Python ругается:
TypeError: 'in <string>' requires string as left operand, not int
Нужно заменить файл app/admin/admin_config.py полностью.


⸻


Исправь 
app/admin/admin_config.py
Вставь туда вот этот код:
import os
from typing import Any


def get_raw_admin_ids() -> Any:
    """
    Берёт ADMIN_IDS из app.config или из .env.

    Поддерживает варианты:
    ADMIN_IDS=123456789
    ADMIN_IDS=123456789,987654321
    ADMIN_IDS="123456789,987654321"
    ADMIN_IDS=[123456789, 987654321]
    """

    try:
        from app.config import ADMIN_IDS

        if ADMIN_IDS:
            return ADMIN_IDS

    except ImportError:
        pass

    return os.getenv("ADMIN_IDS", "")


def parse_admin_ids(raw_admin_ids: Any) -> set[int]:
    """
    Превращает ADMIN_IDS в set[int].
    """

    if raw_admin_ids is None:
        return set()

    if isinstance(raw_admin_ids, int):
        return {raw_admin_ids}

    if isinstance(raw_admin_ids, list | tuple | set):
        result = set()

        for item in raw_admin_ids:
            try:
                result.add(int(str(item).strip()))
            except ValueError:
                continue

        return result

    raw_text = str(raw_admin_ids).strip()

    if not raw_text:
        return set()

    raw_text = raw_text.replace("[", "")
    raw_text = raw_text.replace("]", "")
    raw_text = raw_text.replace('"', "")
    raw_text = raw_text.replace("'", "")

    parts = raw_text.split(",")

    result = set()

    for part in parts:
        part = part.strip()

        if not part:
            continue

        try:
            result.add(int(part))
        except ValueError:
            continue

    return result


def get_admin_ids() -> set[int]:
    raw_admin_ids = get_raw_admin_ids()
    return parse_admin_ids(raw_admin_ids)



def is_admin_user(user_id: int | str | None) -> bool:
    """
    Проверяет, является ли пользователь админом.
    """

    if user_id is None:
        return False

    try:
        user_id_int = int(user_id)
    except ValueError:
        return False

    return user_id_int in get_admin_ids()


⸻


Проверь 
.env
В .env должно быть так:
ADMIN_IDS=123456789
или если несколько админов:
ADMIN_IDS=123456789,987654321
Без квадратных скобок лучше.


⸻


Проверь 
app/config.py
Если у тебя там есть ADMIN_IDS, пусть будет так:
import os

ADMIN_IDS = os.getenv("ADMIN_IDS", "")
Не надо делать так:
ADMIN_IDS = int(os.getenv("ADMIN_IDS"))
и не надо так:
ADMIN_IDS = os.getenv("ADMIN_IDS").split(",")
Пусть admin_config.py сам нормально парсит.


⸻


Быстрая проверка
В терминале из корня проекта:
python -c "from app.admin.admin_config import get_admin_ids; print(get_admin_ids())"
Должно вывести примерно:
{123456789}
Потом проверь:
python -c "from app.admin.admin_config import is_admin_user; print(is_admin_user(123456789))"
Должно быть:
True
После этого перезапусти бота:
python -m app.main
Теперь /admin не должен падать.







7. Быстрая проверка в терминале
python -c "from app.
admin.texts import get_admin_text; print(get_admin_text('btn_add_qa'))"
Должно вывести:
➕ Добавить Q/A
Потом:
python -c "from app.admin.texts import get_admin_text; print(get_admin_text('btn_active_incidents'))"
Должно вывести:
📌 Активные проблемы
Потом:
python -c "from app.admin.texts import get_admin_text; print(get_admin_text('btn_stats'))"
Должно вывести:
📊 Статистика


⸻


8. Перезапусти бота
python -m app.main
После этого /admin должен показать нормальные кнопки:
➕ Добавить Q/A
🚨 Добавить временную проблему
📌 Активные проблемы
🧠 Review cases
🔐 Read-only режим
📊 Статистика
⚙️ Настройки
🚪 Выйти
И кнопки Добавить Q/A, Активные проблемы, Статистика должны начать работать.


!!!!!!!!!!!!!!!!!!!!11



